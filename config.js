const config = {
    db: {
      dialect: "mysql",
      host: "localhost",
      user: "root",
      password: "",
      database: "cab",
      connectTimeout: 60000
    },
    listPerPage: 10,
  };
export default config;
